<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Admin Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet"/>
</head>
<body class="bg-gray-50 min-h-screen">
  <header class="bg-blue-700 text-white p-4 flex justify-between items-center">
    <h1 class="text-xl font-semibold">Admin Dashboard</h1>
    <a href="/admin/logout" class="text-sm underline hover:text-gray-200">Logout</a>
  </header>

  <main class="p-6">
    <h2 class="text-2xl font-bold mb-4 text-gray-800">Submitted Audits</h2>

    <% if (audits.length === 0) { %>
      <p class="text-gray-500">No audits found.</p>
    <% } else { %>
      <div class="overflow-x-auto">
        <table class="min-w-full bg-white shadow rounded-lg overflow-hidden">
          <thead class="bg-gray-100">
            <tr>
              <th class="text-left px-4 py-2">#</th>
              <th class="text-left px-4 py-2">User</th>
              <th class="text-left px-4 py-2">Total Questions</th>
              <th class="text-left px-4 py-2">Submitted On</th>
            </tr>
          </thead>
          <tbody>
            <% audits.forEach((audit, index) => { %>
              <tr class="border-t">
                <td class="px-4 py-2"><%= index + 1 %></td>
                <td class="px-4 py-2"><%= audit.user %></td>
                <td class="px-4 py-2"><%= audit.questions.length %></td>
                <td class="px-4 py-2"><%= new Date(audit.createdAt).toLocaleString() %></td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
    <% } %>
  </main>
</body>
</html>
