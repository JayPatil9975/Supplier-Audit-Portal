<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
      rel="stylesheet"
    />
  </head>
  <body
    class="flex items-center justify-center min-h-screen bg-gradient-to-r from-blue-50 to-indigo-100"
  >
    <div
      class="bg-white p-8 rounded-xl shadow-2xl w-full max-w-md border border-gray-100 transform transition duration-500 hover:scale-105"
    >
      <div class="flex justify-center mb-6">
        <div
          class="h-20 w-20 rounded-full bg-gradient-to-r from-indigo-600 to-blue-500 flex items-center justify-center shadow-lg"
        >
          <i class="fas fa-user-lock text-white text-3xl"></i>
        </div>
      </div>

      <h2 class="text-3xl font-bold text-center mb-6 text-gray-800">
        Welcome Back
      </h2>

      <% if (error) { %>
      <div
        class="bg-red-100 text-red-700 p-3 rounded-lg mb-6 text-center border-l-4 border-red-500 flex items-center gap-2"
      >
        <i class="fas fa-exclamation-circle"></i>
        <%= error %>
      </div>
      <% } %>

      <form action="/login" method="POST" class="space-y-5">
        <div class="relative">
          <div
            class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none"
          >
            <i class="fas fa-envelope text-gray-400"></i>
          </div>
          <input
            type="email"
            name="email"
            placeholder="Your Email"
            class="w-full p-4 pl-10 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition bg-gray-50"
            required
          />
        </div>

        <div class="relative">
          <div
            class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none"
          >
            <i class="fas fa-lock text-gray-400"></i>
          </div>
          <input
            type="password"
            name="password"
            placeholder="Your Password"
            class="w-full p-4 pl-10 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition bg-gray-50"
            required
          />
        </div>

        <button
          type="submit"
          class="w-full bg-gradient-to-r from-indigo-600 to-blue-500 text-white py-3 rounded-lg hover:from-indigo-700 hover:to-blue-600 transition shadow-lg flex items-center justify-center gap-2 font-medium"
        >
          <i class="fas fa-sign-in-alt"></i>
          Sign In
        </button>
        <a
          href="/admin/login"
          class="w-full inline-block text-center bg-gray-800 text-white py-3 rounded-lg hover:bg-gray-900 transition shadow-lg mt-3"
        >
          <i class="fas fa-user-shield mr-2"></i> Login as Admin
        </a>
      </form>

      <p class="mt-8 text-center text-gray-600">
        Don't have an account?
        <a href="/signup" class="text-indigo-600 hover:underline font-medium"
          >Sign up</a
        >
      </p>
    </div>
  </body>
</html>
