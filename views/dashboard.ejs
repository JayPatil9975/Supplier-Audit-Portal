<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body class="bg-gray-100 flex flex-col min-h-screen">
    <!-- Navbar -->
    <nav class="bg-white shadow-md p-4 flex justify-between items-center">
        <div class="flex items-center">
            <img src="/assets/sup-image.0eed1e84cc2c415f47c5.png" alt="Supplier Portal Logo" class="h-8 mr-2">
            <span class="text-lg font-semibold text-gray-800">Supplier Portal</span>
        </div>
        <div class="flex items-center space-x-4">
            <span class="text-gray-600 font-medium"><%= user.fullName %> | <%= user.companyName %></span>
            <a href="/logout" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600 transition">Logout</a>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="flex-1 max-w-6xl mx-auto p-6">
        <h1 class="text-2xl font-bold mb-2 text-gray-800">Welcome to your Dashboard</h1>
        <p class="text-gray-600 mb-6">Manage your quality assessments and view audit reports.</p>

        <!-- Stats Cards -->
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
            <div class="bg-white p-6 shadow-md rounded-lg border border-gray-300">
                <h2 class="text-gray-500 text-lg">Total Audits</h2>
                <p class="text-3xl font-bold text-gray-800"><%= totalAudits %></p>
                <a href="/audit/form" class="text-blue-600 hover:underline">Start new audit</a>
            </div>
            <div class="bg-white p-6 shadow-md rounded-lg border border-gray-300">
                <h2 class="text-gray-500 text-lg">Latest Score</h2>
                <p class="text-3xl font-bold text-gray-800"><%= latestScore %></p>
                <% if (latestAuditId) { %>
                    <a href="/audit/report/<%= latestAuditId %>" class="text-blue-600 hover:underline">View details</a>
                <% } else { %>
                    <span class="text-gray-400">No audits yet</span>
                <% } %>
            </div>
        </div>

        <!-- Graph Visualization -->
        <div class="bg-white p-6 mt-8 shadow-md rounded-lg border border-gray-300">
            <h2 class="text-xl font-semibold text-gray-700 mb-4">Audit Score Trend</h2>
            <canvas id="auditChart"></canvas>
        </div>

        <!-- Quick Actions -->
        <div class="mt-8 grid grid-cols-1 sm:grid-cols-2 gap-6">
            <div class="bg-blue-600 text-white p-6 rounded-lg shadow-md transition hover:bg-blue-700">
                <h2 class="text-lg font-bold">Quality Audit Form</h2>
                <p class="text-sm mb-4">Complete your quality assessment to evaluate supplier performance.</p>
                <a href="/audit/form" class="bg-white text-blue-600 px-4 py-2 rounded hover:bg-gray-200 transition">
                    Start Audit
                </a>
            </div>
            <div class="bg-green-600 text-white p-6 rounded-lg shadow-md transition hover:bg-green-700">
                <h2 class="text-lg font-bold">Audit Reports</h2>
                <p class="text-sm mb-4">View and download your previous audit reports and results.</p>
                <a href="/audit/reports" class="bg-white text-green-600 px-4 py-2 rounded hover:bg-gray-200 transition">
                    View Reports
                </a>
            </div>
        </div>
    </main>

    <!-- Sticky Footer -->
    <footer class="p-4 text-center text-gray-500 bg-white shadow-md mt-auto">
        © 2025 Supplier Quality Management Portal. All rights reserved.
    </footer>

    <!-- Chart.js Script -->
    <script>
        const auditData = <%- JSON.stringify(auditData) %>;

        const ctx = document.getElementById('auditChart').getContext('2d');
        new Chart(ctx, {
            type: 'line',
            data: {
                labels: auditData.map(audit => audit.date),
                datasets: [{
                    label: 'Audit Scores (%)',
                    data: auditData.map(audit => audit.score),
                    borderColor: 'rgb(75, 192, 192)',
                    backgroundColor: 'rgba(75, 192, 192, 0.2)',
                    borderWidth: 2,
                    fill: true
                }]
            },
            options: {
                responsive: true,
                scales: {
                    y: { beginAtZero: true, max: 100 }
                }
            }
        });
    </script>
</body>
</html>
